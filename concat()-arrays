function nonMutatingConcat(original, attach) {
  let combined = original.concat(attach);
  return combined;
  
}
var first = [1, 2, 3];
var second = [4, 5];
console.log(nonMutatingConcat(first, second)); // Returns [1, 2, 3, 4, 5]
